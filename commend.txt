// 进入环境
conda activate torch_env

// 开始任务
bsub \
  -n 1 \
  -q gpu \
  -gpu "num=1" \
  -R "rusage[mem=6GB]" \
  -o task_output.txt \
  "python main.py --test MNIST --epochs 500 --group 0"

// 查看任务
bjobs