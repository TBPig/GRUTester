// 进入环境
conda activate torch_env

// 开始任务
bsub \
  -n 1 \
  -q gpu \
  -gpu "num=1" \
  -R "rusage[mem=6GB]" \
  -o task_output.txt \
  "python main.py --test MNIST --epochs 40 --group 2"

// 查看任务
bjobs


// 任务：
细调hidden_size
调整weight_decay
测试CNN验证
在测一次2层和3层